# Base URL of the read-only public API the site reads at build time.
# Defaults to https://api.transit411.net when unset. On Cloudflare Pages set it under
# Settings -> Environment variables (it's public, not a secret).
PUBLIC_API_BASE=https://api.transit411.net
